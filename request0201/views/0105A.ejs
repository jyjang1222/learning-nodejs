<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!--
		[문제]
			1. 숫자 3개를 입력받는다.
			2. 입력받은 3개의 수를 비교한다.
			3. 가장 큰 수(MAX)를 출력한다.
            num1, num2, num3
	-->
    <%  
        const test = [num1, num2, num3];
        let max = 0;
        if (num1 > max) {
            max = num1;
        }
        if (num2 > max) {
            max = num2;
        }
        if (num3 > max) {
            max = num3;
        }
    %>
    <p><%=test%> <%=test[0]%> <%=test[1]%> <%=test[2]%></p>
    <p>num1: <%=num1%> num2: <%=num2%> num3: <%=num3%></p>
    <p>가장 큰 수: <%=max%></p>
    <a href="0100">메인으로 가기</a>
</body>
</html>